[project]
name = "cbp_fastapi"
version = "0.1.0"
# description = "FastAPI runtime for cbp"
# authors = ["Peter Alexander <makeroftools@creativedevelopment.systems>"]
# license = "MPLv2"
# readme = "README.md"
requires-python = ">=3.14"
dependencies = [
]

[tool.hatch.build.targets.wheel]
packages = ["."]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
