"""
So a task is essentially a declarative means to generate a 
task object in the target language (python for now).

A task is defined by it's list of child tasks and metadata

If cached or source-local --> execute, else fetch and build
"""





